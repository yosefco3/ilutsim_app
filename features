# T02 — תיקון טסטים: test_repositories.py

## בעיה
טסטים ב-`backend/tests/test_repositories.py` נכשלים כי ה-repositories או המודלים השתנו.

## שלבי תיקון
1. הרץ: `cd backend && python -m pytest tests/test_repositories.py -v --tb=short`
2. בדוק: שינויי שדות במודלים (user שם פרטי/שם משפחה, schedule_week status וכו')
3. עדכן נתוני טסט (fixtures) להתאים לסכמה הנוכחית
4. ודא שה-async session setup עובד

## קבצים רלוונטיים
- `backend/tests/test_repositories.py`
- `backend/app/repositories/*.py`
- `backend/app/models/*.py`
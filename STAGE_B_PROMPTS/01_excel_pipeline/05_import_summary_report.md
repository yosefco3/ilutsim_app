# צעד 05 · דוח סיכום ייבוא + ליטוש

## מטרה
לסגור את השלב: אחרי commit להציג למשתמש **דוח סיכום** ברור, ולוודא שהפייפליין כולו חלק מקצה-לקצה.

## קרא קודם
- צעדים 03-04 (preview + commit).

## קבצים (2-4)
- הרחבת `constraints_import_controller.py` — שתגובת ה-commit תכלול סיכום מובנה.
- הרחבת `ImportConstraintsPage.jsx` — תצוגת סיכום אחרי ייבוא + Toast.
- טסטים: עדכון `test_import_commit.py` / `test_import_preview_endpoint.py`.

## תוכן הדוח
- כמה מאבטחים יובאו בהצלחה (נשמרו).
- כמה מהם **נוצרו חדשים** (לא היו קיימים בדף המאבטחים).
- רשימת **שגיאות פרסור** (`errors`) עם הקשר (שם/יום/משמרת).
- לאיזה שבוע יובא (טווח תאריכים).
> בלי "לא זוהו"/"כפולים" — אין דחיית שמות ואין טיפול בכפילויות (ראו השינוי במשימה זו).

## ליטוש (end-to-end)
- ודא נתיב שלם: העלאה → preview → אישור → commit → סיכום → המאבטחים נראים ב"הגשות".
- הודעות שגיאה ידידותיות (קובץ לא-xlsx, גיליון לא בשם `אילוצים`, כותרת בלי טווח תאריכים).
- עדכן את `_SHIFT_PERIODS`/לוגיקת התצוגה אם צריך עקביות מול הייצוא הקיים.

## טסטים
- תגובת commit כוללת ספירות נכונות (יובאו / נוצרו-חדשים / שגיאות) על קובץ הדוגמה.
- מאבטח חדש (שם שלא קיים) → נספר ב"נוצרו חדשים" ונשמר.

הרץ: backend `pytest` (כל הסוויטה) + frontend `npx vitest run`, או `python3 scripts/test_graph.py`.

## קריטריון הצלחה (סיום השלב כולו)
- ✅ כל הטסטים עוברים.
- ✅ **(המשתמש מריץ את התרחיש המלא):** מעלה את `אילוצים_דמו_מלא.xlsx` (36 מאבטחים, כולל חפיפות,
  הערות, לא-זמין/זמין) → רואה טבלה נקייה ומסודרת → מאשר → מקבל דוח סיכום → המאבטחים והזמינות נראים
  ב"הגשות". **זה מסיים את משימה 01.**

## סיום השלב
1. `python3 scripts/test_graph.py` (טסטים + קאברג').
2. `/home/yosef/.local/bin/code-review-graph update`.
3. עדכן `APP_OVERVIEW.md` — endpoints חדשים (`/admin/import/constraints/*`), שירות `constraints_import`,
   ושורה ב"היסטוריית שינויים".
4. קומיט אחרון: `feat(import): import summary report + end-to-end polish`. push.
5. עדכן `_PROGRESS.md`: "✅ All prompts completed". מחק קובץ זה.
6. עבור למשימה `02_activation_profiles` — כתוב לה פרומפטים מלאים ואז בצע.
